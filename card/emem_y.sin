process emem_y = e1, e1 => e1, e1
sqrts = 0.03197690169481715 GeV  # 1 GeV incident electron
n_events = 100000
sample_format = lhef

beams = e1, e1
beams_pol_density = @(1:1:1, 1:-1:-I, -1:-1:1), @(-1)  # y polarized

cuts = all 0.14159265358979312 <= Theta <= 3 [e1]

simulate (emem_y)
